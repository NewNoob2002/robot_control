6051 zsh -c set -u; trial=/tmp/p66-shutdown-trial-3-10s; mkdir -p "$trial"; ip -details -statistics link show can0 > "$trial/can_preflight.txt"; pgrep -af robot-control-zlac-qualification > "$trial/process_preflight.txt" || true; : > "$trial/rk3588_can.log"; candump -ta can0 > "$trial/rk3588_can.log" 2>&1 & pid=$!; echo "$pid" > "$trial/candump.pid"; sleep 1; if ! kill -0 "$pid" 2>/dev/null; then echo capture_start_failed; exit 70; fi; /tmp/robot-control-qualifications/p66-shutdown-10s-9f21aa6c4e2c/robot-control-zlac-qualification --interface can0 --shutdown-once 2:5 --duration-ms 10000 > "$trial/executor.log" 2>&1; rc=$?; echo "$rc" > "$trial/executor.rc"; sleep 1; kill -INT "$pid" 2>/dev/null || true; sleep 1; kill -0 "$pid" 2>/dev/null && kill -TERM "$pid" 2>/dev/null || true; wait "$pid" 2>/dev/null || true; ip -details -statistics link show can0 > "$trial/can_postflight.txt"; pgrep -af robot-control-zlac-qualification > "$trial/process_postflight.txt" || true; sha256sum /tmp/robot-control-qualifications/p66-shutdown-10s-9f21aa6c4e2c/robot-control-zlac-qualification > "$trial/elf_sha256.txt"; printf "executor_rc=%s\nframes=%s\n" "$rc" "$(wc -l < "$trial/rk3588_can.log")"; exit "$rc"
