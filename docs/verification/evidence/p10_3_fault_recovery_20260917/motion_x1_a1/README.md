# F3 A1 — INCOMPLETE, no physical motion

Two-stick operation was impractical for the single operator. Initial throttle with
steering neutral produced left/right3rpm candidates. The independent single-wheel
send gate rejected0f0003000300 with errno13 before any nonzero transmission.
All1121 RPDO targets and all feedback were zero; no X1 bit observed. Operator explicitly
reports wheels had not begun moving, overriding the generic direction/stop checkbox.
OFF/no abnormal sound is recorded.1882 target/JCAN frames match; complete5729-row trace,
36 volatile writes and full baseline restoration verified by audit-no-motion.py.
Application/runner exit1 and original F3 oracle rejection retained. No F3 acceptance.
Both markers consumed. User requests a single-operator input arrangement.
