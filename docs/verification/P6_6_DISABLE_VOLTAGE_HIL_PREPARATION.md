# P6.6 Disable Voltage HIL Preparation

Prepared: 2026-09-09T10:15:35Z. Final status updated 2026-09-10: **HIL PASS**.

The bounded Debug-only command is `--disable-voltage-once 2:5 --duration-ms 10000`. It commands only node-1 channel 2 at `+5 rpm` for at most 10000 ms, then attempts exactly one expedited SDO download `0x6040:00=0x0000` (`601#2B40600000000000`). The expected newer TPDO1 state is Switch On Disabled on both protocol halves. Once the controlword attempt begins, a response or state timeout cannot submit a second Disable Voltage command, Shutdown, or Operation Enabled.

Cleanup writes and verifies both independent targets zero, requires available independent and packed velocity feedback to reach zero, enters and verifies NMT Pre-operational, and restores temporary volatile `0x200F:00` and `0x1017:00` values. It deliberately skips Shutdown after the Disable Voltage attempt because `0x0006` could leave Switch On Disabled and energize the drive again.

Current-source Host Debug and Clang 22.1.8 ASan/UBSan each pass 46/46 tests. Managed-vcan covers both the acknowledged Switch On Disabled transition and a missing-response path, with no second terminal controlword or re-enable. Clang-format and reviewed bugprone checks pass. The locked, no-network RK3588 Debug qualification build completes 69/69 steps. The staged AArch64 ELF SHA-256 is `49a40dc0fabb6c6b2086f9fe56c74378cf02d978cde909a2b5f5a0c365a4b31a`.

The ELF is staged on the RK3588 at `/tmp/robot-control-qualifications/p66-disable-voltage-49a40dc0fabb/robot-control-zlac-qualification`, with the same checksum. Before motion, `can0` was UP at 500000 bit/s and ERROR-ACTIVE with zero bus/error/drop counters. Rust `jcan 1.0.0` self-test, explicit scan of serial `207F346D5650`, and configuration baseline all passed; `can_speed=0C` and termination remains disabled.

One exactly authorized physical attempt ran and produced matching 396-frame RK3588 and JCAN captures. The command was acknowledged and the drive reported raw `0x1460` on both status halves, but the executor returned nonzero because the existing shared decoder recognized only raw `0x1440` as Switch On Disabled. Cleanup completed without Shutdown or re-enable. The decoder and vcan case were then corrected to the hardware-observed value; another physical attempt requires fresh exact authorization. Quick Stop, `0x2000`, `0x605A`, fault reset, reverse motion, EEPROM, brake-output writes, interface changes, periodic traffic, and automatic retry remain excluded.

The corrected shared decoder accepts masked state `0x0060` as Switch On Disabled while retaining the raw statusword. Host Debug and Clang 22.1.8 ASan/UBSan again pass 46/46 tests, the managed-vcan Disable Voltage case uses raw `0x1460`, reviewed bugprone checks pass, and a fresh RK3588 Debug cross-build completes 69/69 steps. The corrected AArch64 ELF SHA-256 is `833548e609bb96b1061154f3c24c21e1e0a734c20060027a40c9da4ba3354be0`.

The operator corrected the physical mapping record: a positive channel-2/right-wheel target rotates the right wheel counter-clockwise, opposite the positive left-wheel direction. Vehicle-forward convention is left positive and right negative; left `+10 rpm` with right `-10 rpm` produces forward travel at 10 rpm. During trial 1 the left wheel remained stationary, both wheels stopped after Disable Voltage, and no abnormal brake action or sound occurred.

The user freshly authorized and reconfirmed one corrected attempt on 2026-09-10. The executor returned zero. RK3588 and persistent JCAN silent capture each recorded the same 393-frame sequence. The one Disable Voltage request was acknowledged in 0.307 ms, raw dual `0x1460` arrived in 31.754 ms, channel-2 measured velocity first read zero in 420.230 ms, and all velocity views were zero by 422.227 ms. Cleanup restored both volatile values without Shutdown, re-enable, or retry; CAN and JCAN postflights passed. The operator confirmed counter-clockwise right-wheel rotation, a stationary left wheel, both wheels stopping after Disable Voltage, and no abnormal brake action or sound. See `evidence/p6_6_20260910_disable_voltage_corrected_trial_1/`.
